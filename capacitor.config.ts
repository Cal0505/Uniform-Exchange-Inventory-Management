import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uniformexchange.inventory',
  appName: 'uniform-ex-inventory-management',
  webDir: 'dist'
};

export default config;
